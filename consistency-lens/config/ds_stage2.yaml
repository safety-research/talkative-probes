train_batch_size: auto
train_micro_batch_size_per_gpu: auto
gradient_accumulation_steps: auto
zero_optimization:
  stage: 2
  overlap_comm: true
  contiguous_gradients: true
bf16:
  enabled: true
fp16:
  enabled: false
# TODO: fill in optimizer and scheduler overrides